#include <iostream>
using namespace std;

int RepeatingElements(int a[], int n)
{
    int temp[n];

    for (int i = 0; i < n; i++)
    {
    }
}

int main()
{
    int n;
    int a[1000];
    cin >> n;

    for (int i = 0; i < n; i++)
    {
        cin >> a[i];
    }

    cout << RepeatingElements(a, n);
}